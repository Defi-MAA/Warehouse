
this.$api.mypost({
  url:"/api/shops/EditDescript",
  success: (res) => {
	console.log(res);
  },
  fail: (res) => {
	console.log(res);
  }
});
async
let obj = await this.$api.ajax('/api/Hotel/GetHotelInfo', {
					
				});
	let userInfo = uni.getStorageSync('userInfo');
setTimeout(() => {
					this.getOrders(navItem)
					//loaded新字段用于表示数据加载完毕，如果为空可以显示空白页
				}, 600);
 uni.showToast({
						 	title: "提交成功!"
						 });
						 setTimeout(() => {
						 	uni.navigateBack();
						 }, 2000);

onNavigationBarButtonTap(e) {
		const index = e.index;
		let that = this;
		if (index === 0) {
			
		}
}

uni.showModal({
    title: '提示',
    content: '这是一个模态弹窗',
    success: function (res) {
        if (res.confirm) {
            console.log('用户点击确定');
        } else if (res.cancel) {
            console.log('用户点击取消');
        }
    }
});

v-for="(image,index) in imageList" :key="index"

   this.$api.ajax('/api/orders/IntegralPay',{payorderno:this.orderInfo});
   
   //find findIndex filter
   //组合数组 
   that.types.map(n => n.name)
   //查询项目
   that.covers.find(n => n.id == id);
   //查询索引
   that.covers.findIndex(n => n.id == item.id)
   结构复制
   that.types = obj.data.reverse();
   //拼接
   that.covers.concat(subset)
   
   list.forEach(item => {
   	let logo = item.logo;
   
   	
   })
   
   async getUserinfo(){
   	if (this.$api.debug) {
   		var info1 = {};
   		info1.openid = this.$api.openid;
   		info1.unionid = this.$api.unionid;
   		uni.setStorage({ //缓存用户登陆状态
   			key: 'wechatinfo',
   			data: info1
   		});
   		that.wechatinfo = info1;
   	} else {
   	
   		// #ifndef MP-WEIXIN
   		//如果为微信端就进行
   		let info = await this.$api.GetWechatInfo();
   		that.wechatinfo = info;
   		//#endif
   		// #ifdef MP-WEIXIN
   		let info1 = await this.$api.GetSmallInfo();
   		that.wechatinfo = info1;
   		//#endif
   	}
   },